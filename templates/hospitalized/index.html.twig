{% extends 'base.html.twig' %}

{% block title %}Hospitalized index{% endblock %}

{% block body %}
    <h1>Hospitalized index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Service</th>
                <th>Bed</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for hospitalized in hospitalizeds %}
            <tr>
                <td>{{ hospitalized.id }}</td>
                <td>{{ hospitalized.service }}</td>
                <td>{{ hospitalized.bed }}</td>
                <td>
                    <a href="{{ path('app_hospitalized_show', {'id': hospitalized.id}) }}">show</a>
                    <a href="{{ path('app_hospitalized_edit', {'id': hospitalized.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_hospitalized_new') }}">Create new</a>
{% endblock %}
